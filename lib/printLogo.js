module.exports = function (program) {
  /* eslint-disable no-unused-vars */
  var colors = require('colors')
  /* eslint-enable no-unused-vars */
  console.log()
  console.log('Ligthouse server listening on port ' + program.port);
  console.log()
}
